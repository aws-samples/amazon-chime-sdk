{
  "name": "chime-sdk-telehealth-widget-demo",
  "version": "1.0.0",
  "scripts": {
    "deploy": "npm run install-all && cd backend && npm run bootstrap && npm run deploy:no-approval && cd ../frontend && npm run deploy",
    "install-all": "npm --prefix ./backend install && npm --prefix ./frontend install",
    "destroy": "cd backend && npm run destroy"
  }
}
